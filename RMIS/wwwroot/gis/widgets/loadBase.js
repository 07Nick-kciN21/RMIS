// ============================================================================
// loadBase.js - 功能載入器模板
// 負責動態載入和管理所有功能模組的生命週期
// ============================================================================

// 功能實例中心
let objCenter = [];

// UI 容器陣列
let leftBag1 = [];      // 左側主面板（支援分頁）
let rightBag1 = [];     // 右側主面板（支援分頁）
let topBag = [];        // 頂部面板
let bottomBag = [];     // 底部面板（支援分頁）
let fullBag = [];       // 全屏面板
let modalBag = [];      // 模態對話框面板
let popoverBag = [];    // 彈出視窗面板
let swipableBag = [];   // 滑動面板（移動端）

// ============================================================================
// 主要載入器物件
// ============================================================================

var loadBase = {
    
    /**
     * 初始化功能載入器
     * @param {Object} appCore - 應用程式核心物件
     */
    init: function (appCore) {
        console.log("開始載入功能模組...");

        // ====================================================================
        // 地圖工具模組初始化區塊
        // ====================================================================
        
        // objCenter.push(mapMode.set(appCore));
        // objCenter.push(maptoolPrint.set(appCore));
        
        // 載入功能特性
        loadFeatures();

        /**
         * 載入功能特性
         * 初始化 UI 系統和容器
         */
        function loadFeatures() {
            console.log("初始化 UI 容器系統...");

            // UI 建立器初始化
            // concatenate.init();
        }
    }
};  

export { loadBase };