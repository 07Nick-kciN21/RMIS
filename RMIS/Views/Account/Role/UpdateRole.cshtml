@model RMIS.Models.Account.Roles.UpdateRoleView
@using System.Linq

@{
    Layout = null;

    var permJson = System.Text.Json.JsonSerializer.Serialize(
        (Model.Permissions ?? new List<RMIS.Models.Account.Roles.UpdatePermission>())
        .Select(p => new {
            id     = p.PermissionId,
            name   = p.PermissionName,
            read   = p.Read,
            create = p.Create,
            update = p.Update,
            delete = p.Delete,
            export = p.Export
        })
    );
}
<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/account/rolePermission.css" />
</head>
<body>
<div class="rp-page">

    <div class="rp-header">
        <div class="rp-title">
            <span class="rp-role-name">修改功能群組</span>
            <span class="rp-label">編輯</span>
        </div>
    </div>

    <div class="rp-form">
        <input type="hidden" id="RoleId" value="@Model.RoleId" />
        <div class="rp-form-field">
            <label>群組名稱</label>
            <input type="text" id="RoleName" class="form-control" value="@Model.RoleName" />
        </div>
        <div class="rp-form-field">
            <label>狀態</label>
            <div class="rp-radio-group">
                <label>
                    <input type="radio" name="Status" value="true"  @(Model.Status ? "checked" : "") /> 啟用
                </label>
                <label>
                    <input type="radio" name="Status" value="false" @(!Model.Status ? "checked" : "") /> 禁止
                </label>
            </div>
        </div>
    </div>

    <div class="rp-content">
        <div id="view-accordion"></div>
    </div>

    <div class="rp-footer">
        <button id="btn-submit" class="btn btn-primary btn-sm">儲存</button>
        <button id="btn-cancel" class="btn btn-secondary btn-sm">取消</button>
    </div>

</div>

<script>
    const permissionsData = @Html.Raw(permJson);
</script>
<script src="~/lib/jquery/dist/jquery.js"></script>
<script src="~/js/account/role/updateRole.js"></script>
</body>
</html>
