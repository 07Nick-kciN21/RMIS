<!DOCTYPE html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <style>
            body {
                display: flex;
                flex-direction: row;
                margin: 0;
                height: 100vh;
            }

            #sidebar {
                background-color: #f4f4f4;
                padding: 10px;
                overflow-y: auto;
                width: 30%;
            }

            #map {
                flex-grow: 1;
            }

            ul {
                list-style: none;
                padding: 0;
            }

            li {
                padding: 5px 0;
                border-bottom: 1px solid #ccc;
            }

            .content-wrapper {
                flex-direction: row;
                width: 100%;
            }
        </style>
        <title>表單視窗</title>
    </head>
    <script src="~/js/admin/StreetViewPhotoMap.js"></script>
    <body>
        <div id="sidebar">
            <h3>經緯度列表</h3>
            <ul id="coordinate-list"></ul>
            <button onclick="submitCoordinates()">提交座標</button>
        </div>
        <div id="map"></div>
    </body>
</html>